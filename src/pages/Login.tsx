import FormErrorAlert from "../components/FormErrorAlert";
import LoginFormSec from "../components/LoginFormSec";

const Login = () => {
  return (
    <main>
      <FormErrorAlert />
      <LoginFormSec />
    </main>
  );
};

export default Login;