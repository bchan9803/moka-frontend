const FormErrorAlert = () => {
  return (
    <div role='alert' className='form-error-alert alert alert-error hidden'>
      <span>Login failed! Please try again.</span>
    </div>
  )
}

export default FormErrorAlert